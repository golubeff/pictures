{
  "firstId" : "<%= @pictures.first.id %>",
  "images" : [
    <% @pictures.each do |picture| %>
      {
        "imageId" : "<%= picture.id %>",
        "imagePreview" : "<%= picture.url(:thumb) %>",
        "imageFullSize" : "<%= picture.url(:full) %>"
      }
      <% if picture != @pictures.last %>,<% end %>
    <% end %>
  ]
}
